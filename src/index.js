// load javascript
import bootstrap from './js/dependencies/bootstrap.min.js'
import Vue from './js/dependencies/vue'

let app = new Vue({
    el: '#app',
    data: {
        title: 'Hello world!'
    }
});
